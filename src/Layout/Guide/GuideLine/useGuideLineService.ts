// import { useState, useContext, useMemo, useCallback } from 'react'
// import { GuideService } from '../useGuideService'
/**
 * 辅助线组件的逻辑
 *
 * @param {}
 * @return
 */
export const useGuideLineService = () => {
  // 注入上层逻辑
  // const guideService = useContext(GuideService)
  // // 存储辅助线的数值
  // const [value, setValue] = useState<number>(0)
  //
  // // 当前辅助线
  // const currentLine = useMemo(
  //   () =>
  //     guideService.lines.find((el) => el === value) || {
  //       title: '',
  //       description: ''
  //     },
  //   [value, guideService]
  // )
  //
  // // 更新辅助线
  // const setCurrentLine = useCallback(
  //   (value: number) => {
  //     guideService.setLines()
  //   },
  //   [value, guideService]
  // )

  return 12313
}
